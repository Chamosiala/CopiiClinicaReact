mutation CreatePrezenta($copilId: Int!, $input: PrezentaInput!) {
  createPrezenta(copilId: $copilId, input: $input) {
    errors {
      ...RegularError
    }
    prezenta {
      ...RegularPrezenta
    }
  }
}
